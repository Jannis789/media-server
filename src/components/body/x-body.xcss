@use '../../styles/global.xcss' as *;

.main-content {
    padding: $small-spacing $content-padding-width;
    
    .content-area {
        display: flex;
        flex-direction: column;
        gap: 16px;
        border-radius: $normal-spacing;
    }
}